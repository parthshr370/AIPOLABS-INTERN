# Portia and ACI Environment Configuration Template
#
# How to use:
# 1. Make a copy of this file and name it .env (e.g., `cp .env.template .env`)
# 2. Fill in the values for each variable below.
# 3. Ensure the .env file is in the same directory as your main scripts (e.g., aci-portia/)
#    or that your scripts load it correctly if it's located elsewhere.
#
# Add .env to your .gitignore file.

# --- Google Generative AI Configuration ---
# Required for Portia to use Google's language models (e.g., Gemini).
# Paste your gemini api key here and configure the model in the config.py
GOOGLE_API_KEY="YOUR_GOOGLE_API_KEY_HERE"

# --- ACI aipolabs app mcp server ---
# Required for using ACI tools via the MCP (Multi-Capability Planner) server.
# How to get:
# configure your apps and get your aci key at - https://platform.aci.dev/apps
ACI_API_KEY="YOUR_ACI_API_KEY_HERE"

